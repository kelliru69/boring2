# Clases iniciales (Class Select)

Coloca aquí archivos `.tres` de tipo **ClassData** (Mage, Swordman).

Si la carpeta está vacía, el juego usa `data/class_select_catalog.gd`.

Wizard / Sage / Knight / Crusader se desbloquean con Job Change en partida, no en este menú.
